{
	"name": "vexa-workers",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"deploy": "wrangler publish src/index.js",
		"dev": "wrangler dev src/index.js --local"
	},
	"devDependencies": {
		"miniflare": "^2.13.0",
		"wrangler": "^2.0.0"
	}
}
